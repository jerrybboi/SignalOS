import Link from "next/link";
import { MetricCard } from "@/components/MetricCard";
import { getSnapshots } from "@/lib/binance";
import { rankSignals } from "@/lib/radar";
import { buildThesis } from "@/lib/thesis";
export const revalidate=60;
const usd=new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:2});
export default async function Home(){const markets=await getSnapshots(["SOL","BNB","ETH","SUI","TAO"]);const signals=rankSignals(markets);const top=signals[0];const theses=markets.map(buildThesis);const valid=theses.filter(t=>t.status==="VALID").length;const weakening=theses.filter(t=>t.status==="WEAKENING").length;return <div className="page"><section className="hero"><div><p className="eyebrow">AGENT ACTIVE · BINANCE MARKET LAYER</p><h1>Markets move.<br/><em>SignalOS watches.</em></h1><p>Your AI market-decision agent detects unusual conditions, investigates the evidence, builds testable theses and checks contextual risk before you act.</p><div className="actions"><Link className="btn primary" href="/radar">See Latest Signals →</Link><a className="btn secondary" href="#workflow">How It Works</a></div></div><div className="hero-orb"><div className="pulse-ring"/><blockquote>“Attention is an edge.”<small>SignalOS</small></blockquote></div></section><section className="metrics"><MetricCard value={String(signals.length)} label="Live Signals" sub="Ranked from current data"/><MetricCard value={String(valid)} label="Valid Theses" sub={`${weakening} weakening`}/><MetricCard value="5" label="Guard Factors" sub="Per risk check"/><MetricCard value={String(markets.length)} label="Assets Monitored" sub="Binance spot markets"/></section><section className="grid-2" id="workflow"><article className="panel"><div className="panel-head"><h2>Agent Brief</h2><span>refreshes ~60s</span></div><p>{top?`${top.symbol} currently ranks highest: ${top.summary} SignalOS found ${top.evidence.length} supporting observations and ${top.risks.length} risk/contradiction checks.`:"Binance market data is temporarily unavailable; SignalOS will retry on refresh."}</p><Link href="/radar" className="text-link">Open Full Radar →</Link></article><article className="panel"><div className="panel-head"><h2>Top Signal Right Now</h2>{top&&<span className="signal-chip">{top.type}</span>}</div>{top?<><h3>{top.symbol} <small>{top.symbol}/USDT</small></h3><div className="price-line"><strong>{usd.format(top.price)}</strong><span className={top.change24h>=0?"positive":"negative"}>{top.change24h>=0?"+":""}{top.change24h.toFixed(2)}%</span></div><div className="confidence"><span style={{width:`${top.confidence}%`}}/></div><small>Condition confidence: {top.confidence}%</small></>:<p>Waiting for fresh market data.</p>}</article></section><section className="grid-2"><article className="panel"><div className="panel-head"><h2>Agent Workflow</h2><Link href="/radar">Start →</Link></div><ul className="activity"><li>Radar detects unusual conditions <span>DETECT</span></li><li>MoveLens separates evidence from interpretation <span>INVESTIGATE</span></li><li>Thesis defines what must remain true <span>BUILD</span></li><li>Guard checks a proposed action against live conditions <span>GUARD</span></li></ul></article><article className="panel quick-guard"><h2>Quick Guard</h2><p>Run a no-execution risk check using current market conditions.</p><Link className="guard-input" href={top?`/guard?symbol=${top.symbol}&direction=buy&amount=200`:"/guard"}>e.g. Buy 200 USDT of {top?.symbol||"SOL"} <b>→</b></Link></article></section></div>}
