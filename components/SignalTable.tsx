import Link from "next/link";
import type { RadarSignal } from "@/lib/radar";
const usd = new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 2 });
export function SignalTable({ signals }: { signals: RadarSignal[] }){return <div className="table-wrap"><table><thead><tr><th>#</th><th>Asset</th><th>Signal</th><th>Price</th><th>24h</th><th>Confidence</th><th>Observed summary</th><th/></tr></thead><tbody>{signals.map((s,i)=><tr key={s.symbol}><td>{i+1}</td><td><b>{s.symbol}</b><small>{s.symbol}/USDT · Binance</small></td><td><span className="signal-chip">{s.type}</span></td><td>{usd.format(s.price)}</td><td className={s.change24h>=0?"positive":"negative"}>{s.change24h>=0?"+":""}{s.change24h.toFixed(2)}%</td><td>{s.confidence}%</td><td>{s.summary}<small>Observed {new Date(s.observedAt).toLocaleTimeString([], {hour:"2-digit",minute:"2-digit"})}</small></td><td><Link className="btn secondary" href={`/investigate/${s.symbol.toLowerCase()}`}>Investigate →</Link></td></tr>)}</tbody></table>{!signals.length&&<div style={{padding:24,color:"var(--muted)"}}>Binance market data is temporarily unavailable. SignalOS will retry on refresh.</div>}</div>}
